# qbank public demo

This tiny bank and its SVG are original qbank project examples released under the repository's
MIT License. They do not reproduce an exam, textbook, user library, or third-party artwork.

From this directory, rebuild the disposable index and validate the question:

```powershell
qbank index rebuild
qbank validate --format json
```
