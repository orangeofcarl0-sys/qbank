# Codex Skill discovery prompts

```text
使用 $qbank，检查当前题库状态。
```

```text
使用 $qbank，把 examples/source-notes.md 中的题目整理为草稿，
先 dry-run，不要正式写入。
```

```text
使用 $qbank，从 reviewed 光学题目中选择一份 40 分测试，
生成 paper.yaml 并导出 Markdown 学生版。
```

```text
检查 optics 题库中答案为空或条件疑似不完整的题目。
```

```text
当前在另一个教材项目中。使用 $qbank，把 notes/chapter-3.md 整理到位于
<qbank-root> 的题库；只做 dry-run，不得修改教材项目。
```

```text
使用 $qbank 整理这份资料，但当前消息没有给出目标题库位置。
```

```text
继续上一任务。交接记录包含目标题库、integration revision、来源、授权、已执行命令、
校验结果和下一步；写入前重新验证这些信息。
```
