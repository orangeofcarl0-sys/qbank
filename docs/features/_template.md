# 功能名称

> Status: proposed
>
> Target release: 0.3.0
>
> Tracking issue: 待填写

## 用户目标

说明用户要完成的任务、目标人群和可观察结果。

## 使用入口

列出用户可以发现并使用该功能的入口；没有入口时明确写“不涉及”及原因。

## CLI / Studio / MCP 对应关系

说明三个界面的等价能力、仅在某一界面存在的交互，以及共享的应用服务边界。

## 数据与配置变化

列出权威文件、Schema、配置、索引、历史或生成产物变化。若无变化，明确说明。

## 安全和失败行为

描述授权、dry-run、事务、回滚、边界检查、诊断码和外部依赖失败行为。

## 兼容性与迁移

说明对现有 CLI、JSON、Markdown、Schema、配置和用户数据的影响，以及是否需要迁移。

## 测试与验收

列出确定性测试、真实交互、安装场景、质量门和完成标准。

## 当前限制

明确记录不支持的场景、性能边界和后续工作，不以计划中的能力描述当前行为。
