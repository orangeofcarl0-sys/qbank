<!doctype html>
<html lang="{{ language }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title }}</title>
  <style>
    body { max-width: 920px; margin: 2rem auto; padding: 0 1rem; font: 16px/1.65 system-ui, sans-serif; color: #172033; }
    img { max-width: 100%; height: auto; }
  </style>
</head>
<body>{{ body_html | safe }}</body>
</html>
